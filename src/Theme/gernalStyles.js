import { makeStyles } from "@material-ui/core";
import colors from "./colors";
import { Headingfonts, TextFonts } from "./fonts";

const useStyles = makeStyles(() => ({
  linkStyle: {
    textDecoration: "none",
    color: "inherit",
  },
}));
export default useStyles;
