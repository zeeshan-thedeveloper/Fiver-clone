import React from 'react';

function ManageMasterDatabase(props) {
    return (
        <div>
            <h1>Manage Master database (Will be done one next version)</h1>
        </div>
    );
}

export default ManageMasterDatabase;