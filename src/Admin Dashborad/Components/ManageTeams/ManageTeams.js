import React from 'react';

function ManageTeams(props) {
    return (
        <div>
            <h1>Manage Team (Will be done in next version)</h1>
        </div>
    );
}

export default ManageTeams;