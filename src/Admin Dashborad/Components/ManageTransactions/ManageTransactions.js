import React from 'react';

function ManageTransactions(props) {
    return (
        <div>
            <h1>Manage Transactions (Here will be the simple list showing all transitions)</h1>
        </div>
    );
}

export default ManageTransactions;